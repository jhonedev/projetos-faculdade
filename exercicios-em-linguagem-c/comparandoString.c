#include <stdio.h>
#include <string.h>

int main() {

    char stringUm[10] = "Alagoas";
    char stringDois[10] = "Alagoas";

    if (strcmp (stringUm, stringDois) == 0)
    {
        printf("String's iguais");
    }
    
}